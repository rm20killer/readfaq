//config file

const { Channel } = require("discord.js");

module.exports ={
    //discord bot token
    BotToken: 'bot token',
    prefix:'&',
    //everything else should be set up
}
//export {BotToken};

